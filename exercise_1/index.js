var productName = "candy";
var price = 2;
var discountAvailable = false;
// function getDiscount(price, discount) {
//   return price - price * discount;
// }
function getDiscount(price, discount) {
    return price - price * discount;
}
console.log(getDiscount(10, 1));
function printLength(x) {
    console.log(x.length);
}
